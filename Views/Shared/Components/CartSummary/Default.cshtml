@model int

<a class="nav-link ts-cart-btn d-inline-flex align-items-center" asp-controller="Cart" asp-action="Index">
    <i class="bi bi-cart3"></i>Sepet
    @if (Model > 0)
    {
        <span class="ts-cart-badge">@Model</span>
    }
</a>
